# ell=2 extended rho atlas r=3..30 report

Input generated by numeric exact Fraction engine from K/M formulas.

## Window

```text
r = 3..30
```

## Support

```text
lemma1: k = 0..r+4
lemma2: k = 0..r+1
```

Total negative rho entries: `0`.

## Diagonal forward-difference audit

Use `m=max_k(r)-k`.

```text
diagonals tested: 67
diagonals with all forward differences >=0: 67/67
```

## Fixed-k sanity

Fixed-k still shows failures in forward differences; diagonal coordinate remains the natural coordinate.

## Main conclusion

The r=3..30 exact atlas preserves the diagonal binomial-positivity signal. This strongly supports the Diagonal Positivity Lemma:

```text
rho_{max_k(r)-m}(r) in R_+[binom(r-r0(m), i)]
```

for each fixed top-boundary diagonal `m`.
