# Tantrium Benchmark Report

The research OS benchmark suite checks that the machine is not a blind `PASS` emitter.

Benchmarks:

- RH remains internally closed.
- Hankel remains proven by certificate.
- Goldbach remains blocked at the named minor arc gap.
- Lah produces a refined subgap or a proof artifact.
- coefficient positivity identifies and refines the first frontier.
- A deliberately false positivity conjecture produces a counterexample artifact.
- A deliberately missing theorem graph node produces a missing-dependency result.

Generated outputs:

- `inputs/benchmarks/`
- `results/benchmarks/benchmark_report.json`
- `results/benchmarks/benchmark_report.md`
- `results/benchmarks/counterexample_false_positive_quadratic.json`
