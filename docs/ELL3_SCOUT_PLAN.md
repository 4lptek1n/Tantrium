# ell=3 Scout Plan

## Aim

Start the ell=3 layer using the architecture discovered in ell=2.

The ell=2 lesson is:

```text
Do not hunt scalar ratios first.
First build the quotient, rho atlas, natural diagonal coordinate, and non-circular production operator.
```

## ell=3 workflow

1. derive the ell=3 cumulant kernel;
2. reduce the kernel to R_j / q_d language;
3. find the quotient factor in the coefficient polynomial;
4. build the rho atlas;
5. identify the natural coordinate;
6. search for non-circular production;
7. isolate the residual theorem.

## Expected shape

ell=3 will likely have a higher residual structure than ell=2.

Expected chain:

```text
ell=3 cumulant kernel
=> quotient factor
=> rho-like coefficient array
=> atlas coordinate
=> production operator
=> residual theorem
```

## Warning from ell=2

A too-strong PF / real-rootedness route may fail. The correct target may again be direct coefficient positivity after quotient removal.

## First task

Write the ell=3 log-cumulant kernel as a partition sum over total weight 6.
