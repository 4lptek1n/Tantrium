# Proof Foundry scan trigger

This file intentionally triggers the Tantrium Proof Foundry GitHub Actions scan workflow.

Default scan command:

```bash
python tools/tantrium.py certify --scan all --max-ell 5 --model qdiff
```
